import React from 'react';

import ProfileHeader from '../../hoc/profile_header'

const UserProfile = () => {
    return (
        <ProfileHeader>
        UserProfile
        </ProfileHeader>
    );
};

export default UserProfile;